/**
 * Centralized API Configuration
 * ─────────────────────────────
 * Replace the value below with your deployed backend URL
 * after deploying the backend (packages/backend) to a cloud provider.
 *
 * Example (Railway / Render / EC2):
 *   const API_BASE = 'https://api.dnyansiddhigurukul.in';
 */
const API_BASE = 'REPLACE_AFTER_BACKEND_DEPLOY';
